name: Lunch Reminder

on:
  schedule:
    # UTC time. Beijing is UTC+8.
    # Target: 12:30 CST | Trigger: 12:10 CST (UTC 04:10) to buffer delay
    - cron: '10 4 * * *'
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Lunch Reminder
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
        run: |
          # Force type to lunch regardless of execution time
          python src/main.py lunch
